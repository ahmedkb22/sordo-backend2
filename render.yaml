services:
  - type: web
    name: sordo-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn sordo.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
